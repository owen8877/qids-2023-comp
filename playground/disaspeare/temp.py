from xarray import Dataset, DataArray

X: Dataset
X_transformed: Dataset = wrapped_estimator.fit_transform(X[self.feature])

xr.concat

# https://phausamann.github.io/sklearn-xarray/content/wrappers.html